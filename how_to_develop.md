## Git branches

- "upstream": This branch is used to sync the upstream repository into the forked repository. This branch is read only.
- "main": This is the main branch. It contains the ported version of inav. This branch does not allow direct pushes. Changes must be pushed to a different branch and can then be merged into the main branch.
- "devel": This branch is used for development.